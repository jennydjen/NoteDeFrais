{# empty Twig template #}

{% extends "JennyUserBundle::layout.html.twig" %}

{% block body %}
    {% if data %}
        <table class="table table-striped table-bordered table-hover" style="width: 200px;" align="center">
            {% for keyr, row in data %}
                <tr>
                    {% for keyc, column in row %}
                        <td>{{ keyc }}{{ keyr }} : {{  column }} <td>
                    {% endfor %}
                </tr>
            {% endfor %}
        </table>
    {% endif %}
{% endblock %}